{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Axiom Inference Engine","text":"<p>One binary. Everything inside.</p> <p>The Axiom Inference Engine packages, serves, and fine-tunes <code>.axim</code> models \u2014 self-contained model files that bundle weights, config, tokenizer, and metadata into a single file.</p> <p>Get started{ .md-button .md-button--primary } CLI reference{ .md-button }</p>"},{"location":"#what-you-get","title":"What you get","text":"- :material-package-variant: **The `.axim` format**      A single binary file with safetensors weights, JSON config, pickled tokenizer, and JSON metadata. No scattered files. No missing pieces.  - :material-console: **Unified CLI**      `axim download`, `axim infer`, `axim serve`, `axim export`, `axim prepare-data`, and `axim sft` all share the same dispatch pattern.  - :material-api: **OpenAI-compatible API**      `axim serve` exposes `/v1/chat/completions`, `/v1/completions`, `/v1/models`, and `/v1/debug/template` with streaming SSE support.  - :material-web: **Browser Web UI**      `webui/index.html` is a self-contained dev console that talks directly to the running API server \u2014 no build step required.  - :material-tune: **SFT pipeline**      Full-model fine-tuning with best-fit packing, assistant-only loss masking, Muon or AdamW optimizers, and periodic checkpoints."},{"location":"#why-axim","title":"Why axim?","text":"<p>Most model packaging formats handle one part of the problem well but leave you to duct-tape the rest together. Safetensors is fast and memory-mappable, but it is only weights. GGUF is purpose-built for llama.cpp. Zip files work, but they are not structured for model loading.</p> <p><code>.axim</code> keeps the speed and safety of safetensors for the weights section, while adding a small header and section table so the config, tokenizer, and metadata travel with the model. Load one file and you have everything needed for inference, serving, or fine-tuning.</p>"},{"location":"#quick-links","title":"Quick links","text":"<ul> <li>Install the package</li> <li>Run your first inference</li> <li>Read the <code>.axim</code> format spec</li> <li>Understand why <code>.axim</code> exists</li> </ul>"},{"location":"webui/","title":"Web UI","text":"<p><code>webui/index.html</code> is a self-contained dev console. It has no build step and no backend of its own; it talks to the <code>axim serve</code> REST API. Open the file directly in a browser.</p>"},{"location":"webui/#quick-start","title":"Quick start","text":"<ol> <li>Start the API server:</li> </ol> <pre><code>axim serve --axim model.axim --device cuda\n</code></pre> <ol> <li>Open <code>webui/index.html</code> in a browser.</li> <li>Switch to Advanced mode (top bar) and set the endpoint, e.g.    <code>http://localhost:8000</code>.</li> <li>Click test connection. The status dot turns green and the model id appears    next to it when the UI can reach <code>/health</code> and <code>/v1/models</code>.</li> </ol>"},{"location":"webui/#simple-mode","title":"Simple mode","text":"<p>Simple mode is a single chat thread.</p> <ul> <li>Conversations are stored in <code>localStorage</code> under the keys <code>axim.convos</code> and   <code>axim.activeId</code>.</li> <li>The sidebar lists all saved conversations, sorted by most recent.</li> <li>Click a conversation to reopen it.</li> <li>Hover a conversation to rename or delete it.</li> <li>Click the top bar title to rename the current conversation.</li> <li>Enter sends a message. Shift+Enter inserts a newline.</li> <li>The New chat button starts a fresh thread.</li> </ul>"},{"location":"webui/#advanced-mode","title":"Advanced mode","text":"<p>Toggle Advanced in the top bar to open the right-hand panel.</p>"},{"location":"webui/#connection","title":"Connection","text":"<ul> <li>Endpoint \u2014 URL of the <code>axim serve</code> API.</li> <li>test connection \u2014 hits <code>/health</code> and <code>/v1/models</code>.</li> </ul>"},{"location":"webui/#generation-controls","title":"Generation controls","text":"<ul> <li>temperature \u2014 sampling temperature; <code>0</code> is greedy.</li> <li>top-k \u2014 top-k filtering (blank defaults to <code>50</code>).</li> <li>max tokens \u2014 maximum tokens to generate.</li> <li>repetition penalty \u2014 HF-style repetition penalty.</li> <li>system prompt \u2014 prepended as a <code>system</code> message in chat mode.</li> <li>stop sequences \u2014 comma-separated strings that stop generation.</li> <li>stream tokens \u2014 when on, tokens stream in via SSE.</li> </ul>"},{"location":"webui/#generation-mode","title":"Generation mode","text":"<ul> <li>Chat \u2014 sends <code>messages</code> to <code>/v1/chat/completions</code>.</li> <li>Raw completion \u2014 sends the composer text verbatim to <code>/v1/completions</code>   with a BOS prepended by the server.</li> </ul>"},{"location":"webui/#preview-prompt-template","title":"Preview prompt template","text":"<p>The preview prompt template button hits <code>/v1/debug/template</code> and shows:</p> <ul> <li>the rendered chat text,</li> <li>the token id list,</li> <li>the token count.</li> </ul> <p>This is useful for verifying exactly how the tokenizer turns messages into ids.</p>"},{"location":"webui/#streaming-status-and-usage","title":"Streaming status and usage","text":"<p>While generating, a status line under the composer shows:</p> <pre><code>generating \u00b7 N tok \u00b7 X tok/s\n</code></pre> <p>When finished:</p> <pre><code>done \u00b7 N tok in Y.Ys \u00b7 X tok/s\n</code></pre> <p>In Advanced mode, each assistant message displays usage stats:</p> <pre><code>P\u2191 C\u2193 (T)\n</code></pre> <p>where <code>P</code> is prompt tokens, <code>C</code> is completion tokens, and <code>T</code> is total tokens.</p>"},{"location":"webui/#message-actions","title":"Message actions","text":"<ul> <li>regenerate (assistant messages) \u2014 delete the assistant reply and generate it   again from the last user message.</li> <li>edit &amp; resend (user messages) \u2014 load the message back into the composer,   truncate the thread to before that message, and resend after editing.</li> <li>copy \u2014 copy the message text to the clipboard.</li> </ul>"},{"location":"webui/#mobile-layout","title":"Mobile layout","text":"<p>On narrow screens the sidebar and advanced panel become floating overlays that slide in/out instead of fixed columns.</p>"},{"location":"api/python/","title":"Python API reference","text":"<p>Import the public API from the <code>axim</code> package:</p> <pre><code>from axim import load_axim, load_model, load_from_dir, Tokenizer, sample\n</code></pre> <p>The Python API is a thin layer over the <code>.axim</code> container format and the vendored nanoChat model code. It is the same code path used by the <code>axim</code> CLI.</p>"},{"location":"api/python/#load_aximpath","title":"<code>load_axim(path)</code>","text":"<p>Read an <code>.axim</code> file and return its raw sections.</p> <pre><code>data = load_axim(\"model.axim\")\n\ncfg      = data[\"config\"]      # dict with architecture hyperparameters\nweights  = data[\"weights\"]     # dict[str, torch.Tensor] (safetensors-backed)\nenc      = data[\"tokenizer\"]   # tiktoken Encoding\nmeta     = data[\"metadata\"]    # dict (may be empty)\n</code></pre> <p>Use this when you only need the contents of the file, not a runnable model.</p>"},{"location":"api/python/#load_modelaxim_path-device-dtypenone","title":"<code>load_model(axim_path, device, dtype=None)</code>","text":"<p>Build a nanoChat <code>GPT</code> from an <code>.axim</code> file and move it to <code>device</code>.</p> <pre><code>import torch\n\nmodel, enc, cfg = load_model(\"model.axim\", device=torch.device(\"cuda\"), dtype=torch.bfloat16)\n</code></pre> <p>Returns:</p> <ul> <li><code>model</code> \u2014 <code>axim._nanochat.gpt.GPT</code> in <code>eval()</code> mode.</li> <li><code>enc</code> \u2014 raw tiktoken <code>Encoding</code>.</li> <li><code>cfg</code> \u2014 model config dict (vocab size, hidden size, layers, sequence length, etc.).</li> </ul> <p>The model is created with freshly initialized weights and then its state dict is overwritten from the <code>.axim</code> weights. Passing <code>dtype</code> casts the parameters after loading.</p>"},{"location":"api/python/#load_from_dirmodel_dir-device-dtypenone","title":"<code>load_from_dir(model_dir, device, dtype=None)</code>","text":"<p>Same as <code>load_model</code>, but loads from a directory containing:</p> <ul> <li><code>model.safetensors</code></li> <li><code>config.json</code></li> <li><code>tokenizer.pkl</code> (looked for in <code>model_dir</code> or its parent)</li> </ul> <pre><code>model, enc, cfg = load_from_dir(\"./model-safetensors\", device=\"cuda\")\n</code></pre>"},{"location":"api/python/#tokenizer","title":"<code>Tokenizer</code>","text":"<p>Wraps the tiktoken encoding stored in the <code>.axim</code> file.</p> <pre><code>tok = Tokenizer(enc)\n</code></pre>"},{"location":"api/python/#methods-and-properties","title":"Methods and properties","text":"Member Description <code>tok.bos</code> Integer id of <code>&lt;|bos|&gt;</code>. <code>tok.encode(text)</code> Encode ordinary text to a list of token ids. <code>tok.decode(ids)</code> Decode a list of token ids to a string. <code>tok.encode_special(\"&lt;|assistant_start|&gt;\")</code> Encode a single registered special token. <code>tok.render_chat(messages, max_len=2048)</code> Render a chat conversation to token ids. <p><code>render_chat</code> produces the nanoChat chat scaffold:</p> <pre><code>ids = tok.render_chat([\n    {\"role\": \"system\", \"content\": \"You are a helpful assistant.\"},\n    {\"role\": \"user\", \"content\": \"Hello!\"},\n], max_len=cfg[\"max_position_embeddings\"])\n\n# Prime generation by appending the assistant-start token.\nids.append(tok.encode_special(\"&lt;|assistant_start|&gt;\"))\n</code></pre> <p>A leading system message is merged into the first user turn (nanoChat convention). Conversations must alternate <code>user</code> / <code>assistant</code> starting with <code>user</code>.</p>"},{"location":"api/python/#sample","title":"<code>sample</code>","text":"<p>Autoregressive sampling loop shared by the API server and <code>axim infer</code>.</p> <pre><code>status = {}\nfor token_id in sample(\n    model, tok, prompt_ids,\n    max_tokens=256,\n    temperature=0.7,\n    top_k=50,\n    device=torch.device(\"cuda\"),\n    repetition_penalty=1.0,\n    stop_strings=[\"\\n\\nuser:\"],\n    status=status,\n):\n    print(tok.decode([token_id]), end=\"\", flush=True)\n\nprint(\"\\nfinish:\", status)\n</code></pre> <p><code>status</code> is populated when generation ends. Common reasons:</p> <ul> <li><code>stop</code> with cause <code>&lt;|assistant_end|&gt; generated</code></li> <li><code>stop</code> with cause matching a stop string</li> <li><code>length</code> when <code>max_tokens</code> is reached</li> </ul> <p><code>temperature &lt;= 0</code> runs greedy decoding. <code>top_k</code> filters the sampling distribution. There is no KV cache; every token performs a full forward pass.</p>"},{"location":"api/rest/","title":"OpenAI-compatible REST API","text":"<p><code>axim serve</code> starts a FastAPI server with a subset of the OpenAI completion endpoints. The server prints a timestamped line for every request showing the endpoint, sampling parameters, prompt length, and completion statistics.</p>"},{"location":"api/rest/#start-the-server","title":"Start the server","text":"<pre><code>axim serve --axim model.axim --device cuda --port 8000\n</code></pre> <p>Omit <code>--port</code> to bind to a random free port in the 8000-9000 range. The examples below assume <code>--port 8000</code>.</p>"},{"location":"api/rest/#endpoints","title":"Endpoints","text":"Method Path Description GET <code>/v1/models</code> List the loaded model. POST <code>/v1/completions</code> Raw prompt to completion. POST <code>/v1/chat/completions</code> Chat-formatted completion. POST <code>/v1/debug/template</code> Render the chat template for messages; no forward pass. GET <code>/health</code> Health check."},{"location":"api/rest/#completion-parameters","title":"Completion parameters","text":"<p>Both <code>/v1/completions</code> and <code>/v1/chat/completions</code> accept:</p> <ul> <li><code>max_tokens</code> (default <code>256</code>)</li> <li><code>temperature</code> (default <code>0.7</code>; <code>0</code> = greedy)</li> <li><code>top_k</code> (default <code>50</code>; <code>None</code> = disabled when greedy)</li> <li><code>repetition_penalty</code> (default <code>1.0</code>)</li> <li><code>stop</code> \u2014 string or list of strings</li> <li><code>stream</code> \u2014 <code>true</code> for SSE token chunks</li> </ul> <p>For <code>/v1/chat/completions</code>, send OpenAI-style <code>messages</code> and the server renders them with <code>Tokenizer.render_chat</code>, then appends <code>&lt;|assistant_start|&gt;</code> before generating.</p>"},{"location":"api/rest/#important-no-kv-cache","title":"Important: no KV cache","text":"<p>Every generated token runs a full forward pass over the full context. This is fine for small models on a GPU but slow on CPU or at long contexts.</p>"},{"location":"api/rest/#examples","title":"Examples","text":""},{"location":"api/rest/#chat-completion-non-streaming","title":"Chat completion (non-streaming)","text":"Linux / macOSWindows PowerShell <pre><code>curl http://localhost:8000/v1/chat/completions \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"model\": \"axim\",\n    \"messages\": [{\"role\": \"user\", \"content\": \"hello\"}],\n    \"max_tokens\": 50,\n    \"temperature\": 0.7\n  }'\n</code></pre> <pre><code>Invoke-RestMethod -Uri \"http://localhost:8000/v1/chat/completions\" `\n  -Method POST `\n  -ContentType \"application/json\" `\n  -Body '{\n    \"model\": \"axim\",\n    \"messages\": [{\"role\": \"user\", \"content\": \"hello\"}],\n    \"max_tokens\": 50,\n    \"temperature\": 0.7\n  }'\n</code></pre> <p>Response:</p> <pre><code>{\n  \"id\": \"chatcmpl-...\",\n  \"object\": \"chat.completion\",\n  \"created\": 1234567890,\n  \"model\": \"axim\",\n  \"choices\": [\n    {\n      \"index\": 0,\n      \"message\": {\"role\": \"assistant\", \"content\": \"Hello! How can I help?\"},\n      \"finish_reason\": \"stop\"\n    }\n  ],\n  \"usage\": {\n    \"prompt_tokens\": 6,\n    \"completion_tokens\": 6,\n    \"total_tokens\": 12\n  }\n}\n</code></pre>"},{"location":"api/rest/#chat-completion-streaming","title":"Chat completion (streaming)","text":"<p>Add <code>\"stream\": true</code> to receive Server-Sent Events. The Linux/macOS example uses curl; PowerShell does not stream SSE conveniently, so use the non-streaming example above for Windows.</p> Linux / macOSWindows PowerShell <pre><code>curl http://localhost:8000/v1/chat/completions \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"model\": \"axim\",\n    \"messages\": [{\"role\": \"user\", \"content\": \"Count to five\"}],\n    \"max_tokens\": 50,\n    \"temperature\": 0.7,\n    \"stream\": true\n  }'\n</code></pre> <pre><code>Invoke-RestMethod -Uri \"http://localhost:8000/v1/chat/completions\" `\n  -Method POST `\n  -ContentType \"application/json\" `\n  -Body '{\n    \"model\": \"axim\",\n    \"messages\": [{\"role\": \"user\", \"content\": \"Count to five\"}],\n    \"max_tokens\": 50,\n    \"temperature\": 0.7,\n    \"stream\": true\n  }'\n</code></pre> <p>The server yields chunks like:</p> <pre><code>data: {\"id\":\"chatcmpl-...\",\"object\":\"chat.completion.chunk\",\"choices\":[{\"delta\":{\"role\":\"assistant\"}}]}\n\ndata: {\"id\":\"chatcmpl-...\",\"object\":\"chat.completion.chunk\",\"choices\":[{\"delta\":{\"content\":\"1\"}}]}\n\ndata: {\"id\":\"chatcmpl-...\",\"object\":\"chat.completion.chunk\",\"choices\":[{\"delta\":{\"content\":\", 2\"}}]}\n...\ndata: [DONE]\n</code></pre>"},{"location":"api/rest/#raw-completion","title":"Raw completion","text":"Linux / macOSWindows PowerShell <pre><code>curl http://localhost:8000/v1/completions \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"model\": \"axim\",\n    \"prompt\": \"Once upon a time\",\n    \"max_tokens\": 50,\n    \"temperature\": 0.7,\n    \"stop\": [\"\\n\"]\n  }'\n</code></pre> <pre><code>Invoke-RestMethod -Uri \"http://localhost:8000/v1/completions\" `\n  -Method POST `\n  -ContentType \"application/json\" `\n  -Body '{\n    \"model\": \"axim\",\n    \"prompt\": \"Once upon a time\",\n    \"max_tokens\": 50,\n    \"temperature\": 0.7,\n    \"stop\": [\"`n\"]\n  }'\n</code></pre>"},{"location":"api/rest/#debug-template","title":"Debug template","text":"<pre><code>curl http://localhost:8000/v1/debug/template \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"messages\": [\n      {\"role\": \"system\", \"content\": \"You are a bot.\"},\n      {\"role\": \"user\", \"content\": \"hi\"}\n    ]\n  }'\n</code></pre> <p>Response:</p> <pre><code>{\n  \"token_ids\": [3, 4, ...],\n  \"decoded\": \"&lt;|bos|&gt; &lt;|user_start|&gt; You are a bot.\\n\\nhi &lt;|user_end|&gt; ...\",\n  \"count\": 12\n}\n</code></pre>"},{"location":"api/rest/#list-models","title":"List models","text":"<pre><code>curl http://localhost:8000/v1/models\n</code></pre>"},{"location":"api/rest/#health-check","title":"Health check","text":"<pre><code>curl http://localhost:8000/health\n</code></pre>"},{"location":"cli/","title":"axim CLI Reference","text":"<p>The <code>axim</code> package exposes a single unified command-line interface. Every subcommand is invoked as <code>axim &lt;command&gt;</code> and is also available as <code>python -m axim &lt;command&gt;</code>.</p>"},{"location":"cli/#running-commands","title":"Running commands","text":"<pre><code>axim &lt;command&gt; [args...]\npython -m axim &lt;command&gt; [args...]\n</code></pre> <p>For help on any command, append <code>--help</code>:</p> <pre><code>axim serve --help\naxim infer --help\n</code></pre> <p>Discover commands</p> <p>Run <code>axim --help</code> to list every subcommand with a one-line description.</p>"},{"location":"cli/#subcommands","title":"Subcommands","text":"Command Purpose <code>axim serve</code> Serve an <code>.axim</code> model behind an OpenAI-compatible API <code>axim inspect</code> Print the internal layout of an <code>.axim</code> file <code>axim export</code> Pack a model directory (safetensors + config + tokenizer) into <code>.axim</code> <code>axim download</code> Download a model from the Hugging Face Hub and produce <code>.axim</code> <code>axim infer</code> Run raw or chat-mode text generation <code>axim prepare-data</code> Turn a Hub dataset into SFT-ready JSONL train/val files <code>axim sft</code> Fine-tune an <code>.axim</code> model on supervised chat data"},{"location":"cli/#quick-start","title":"Quick start","text":"<pre><code># download a base model\naxim download --repo my-org/my-model --output model.axim\n\n# ask it a question\naxim infer --axim model.axim --chat --prompt \"What is 2+2?\"\n\n# serve it as a local API\naxim serve --axim model.axim --port 8000\n</code></pre>"},{"location":"cli/download/","title":"axim download","text":"<p>Download a model from the Hugging Face Hub and produce a single <code>.axim</code> file.</p>"},{"location":"cli/download/#usage","title":"Usage","text":"<pre><code>axim download --repo &lt;repo&gt; --output &lt;path&gt; [--filename &lt;name&gt;] [--model-dir &lt;dir&gt;] [--keep-stage]\n</code></pre>"},{"location":"cli/download/#flags","title":"Flags","text":"Flag Default Description <code>--repo</code> required Hugging Face repository id <code>--output</code> required Destination <code>.axim</code> path <code>--filename</code> <code>None</code> Specific <code>.axim</code> file to download when the repo already contains one <code>--model-dir</code> <code>None</code> Staging directory; when omitted a temporary directory is used <code>--keep-stage</code> <code>False</code> Keep the staging directory after packing (only applies when <code>--model-dir</code> is omitted)"},{"location":"cli/download/#two-download-paths","title":"Two download paths","text":""},{"location":"cli/download/#direct-axim-download","title":"Direct <code>.axim</code> download","text":"<p>If the repository already contains <code>.axim</code> files, the requested (or first) <code>.axim</code> is downloaded directly and copied to <code>--output</code>.</p> <pre><code>axim download --repo my-org/my-model --output model.axim\n</code></pre>"},{"location":"cli/download/#safetensors-staging-merge-and-pack","title":"Safetensors staging, merge, and pack","text":"<p>If no <code>.axim</code> is present, the command:</p> <ol> <li>Stages all repo files into a local directory</li> <li>Merges sharded safetensors when <code>model.safetensors.index.json</code> exists</li> <li>Loads weights, config, tokenizer, and optional <code>nanochat_metadata.json</code></li> <li>Packs everything into the output <code>.axim</code></li> <li>Removes the staging directory unless <code>--keep-stage</code> is set</li> </ol>"},{"location":"cli/download/#example","title":"Example","text":"<pre><code>axim download --repo my-org/my-model --output model.axim --keep-stage\n</code></pre> <p>Disk space</p> <p>Staged repos contain safetensors shards, config, and tokenizer. Ensure you have enough free disk space, or provide <code>--model-dir</code> to control the staging location.</p>"},{"location":"cli/export/","title":"axim export","text":"<p>Pack a model directory into a single <code>.axim</code> file.</p> <p>The source directory must contain <code>model.safetensors</code> and <code>config.json</code>. The tokenizer can be supplied explicitly or discovered automatically.</p>"},{"location":"cli/export/#usage","title":"Usage","text":"<pre><code>axim export --model-dir &lt;dir&gt; --output &lt;path&gt; [--tokenizer-pkl &lt;path&gt; | --repo-id &lt;id&gt;]\n</code></pre>"},{"location":"cli/export/#flags","title":"Flags","text":"Flag Default Description <code>--model-dir</code> required Directory containing <code>model.safetensors</code> and <code>config.json</code> <code>--tokenizer-pkl</code> <code>None</code> Path to a <code>tokenizer.pkl</code> file <code>--repo-id</code> <code>None</code> Hugging Face repo id to download <code>tokenizer.pkl</code> from <code>--output</code> required Destination <code>.axim</code> path"},{"location":"cli/export/#tokenizer-resolution","title":"Tokenizer resolution","text":"<p>The command looks for a tokenizer in this order:</p> <ol> <li><code>--tokenizer-pkl</code> if given</li> <li><code>--repo-id</code> if given (downloads <code>tokenizer.pkl</code> from the Hub)</li> <li><code>model_dir/tokenizer.pkl</code></li> <li><code>model_dir/../tokenizer.pkl</code></li> </ol> <p>If none are found, export fails.</p>"},{"location":"cli/export/#metadata-auto-detection","title":"Metadata auto-detection","text":"<p>If <code>model_dir/nanochat_metadata.json</code> exists, it is read automatically and embedded in the <code>.axim</code> package as model metadata.</p>"},{"location":"cli/export/#example","title":"Example","text":"<pre><code>axim export \\\n    --model-dir ./my_model \\\n    --output my_model.axim\n</code></pre> <p>Explicit tokenizer from the Hub</p> <pre><code>axim export --model-dir ./my_model --repo-id my-org/my-model --output model.axim\n</code></pre>"},{"location":"cli/infer/","title":"axim infer","text":"<p>Run text generation on an <code>.axim</code> model or a raw model directory.</p>"},{"location":"cli/infer/#usage","title":"Usage","text":"<pre><code>axim infer (--axim &lt;path&gt; | --model-dir &lt;dir&gt;) [flags]\n</code></pre>"},{"location":"cli/infer/#flags","title":"Flags","text":"Flag Default Description <code>--axim</code> <code>None</code> Path to the <code>.axim</code> package to load <code>--model-dir</code> <code>None</code> Path to a directory with <code>model.safetensors</code> and <code>config.json</code> <code>--prompt</code> <code>Once upon a time</code> Input prompt text <code>--max-tokens</code> <code>50</code> Maximum number of new tokens to generate <code>--temperature</code> <code>0.7</code> Sampling temperature <code>--top-k</code> <code>50</code> Top-k sampling cutoff <code>--repetition-penalty</code> <code>1.0</code> Repetition penalty; <code>1.0</code> disables it <code>--device</code> <code>cpu</code> Device to run on (<code>cpu</code>, <code>cuda</code>, <code>mps</code>, etc.) <code>--chat</code> <code>False</code> Use chat mode with special tokens <code>--system-prompt</code> <code>None</code> System prompt prepended in chat mode <code>--stream</code> <code>True</code> Stream generated tokens to the terminal as they arrive <code>--no-stream</code> \u2014 Disable streaming; collect the full completion before printing"},{"location":"cli/infer/#streaming-output","title":"Streaming output","text":"<p>By default <code>axim infer</code> streams each generated token to the terminal as soon as it is produced. The prompt is printed first, then tokens are appended in real time, followed by a final status line showing token count, elapsed time, and tokens per second.</p> <p>To fall back to the old collect-then-print behavior, pass <code>--no-stream</code>.</p>"},{"location":"cli/infer/#chat-mode","title":"Chat mode","text":"<p>When <code>--chat</code> is set, the prompt is rendered as a conversation and the model is primed with <code>&lt;|assistant_start|&gt;</code>. Generation stops when the model emits <code>&lt;|assistant_end|&gt;</code>.</p>"},{"location":"cli/infer/#example","title":"Example","text":"<pre><code>axim infer --axim model.axim \\\n    --chat \\\n    --system-prompt \"You are a helpful assistant.\" \\\n    --prompt \"What is the capital of France?\" \\\n    --max-tokens 100 \\\n    --temperature 0.8\n</code></pre> <p>Repetition penalty</p> <p>Values above <code>1.0</code> reduce the likelihood of repeating tokens. The default of <code>1.0</code> leaves logits unchanged.</p>"},{"location":"cli/inspect/","title":"axim inspect","text":"<p>Inspect an <code>.axim</code> package and print a human-readable summary of its contents.</p>"},{"location":"cli/inspect/#usage","title":"Usage","text":"<pre><code>axim inspect &lt;path&gt;\n</code></pre>"},{"location":"cli/inspect/#flags","title":"Flags","text":"Argument Default Description <code>path</code> required Path to the <code>.axim</code> file"},{"location":"cli/inspect/#output","title":"Output","text":"<p>The command prints:</p> <ul> <li>Header information (magic, version, offset table)</li> <li>Section list (weights, config, tokenizer, metadata)</li> <li>A config summary (model size, vocabulary, sequence length, attention details)</li> </ul>"},{"location":"cli/inspect/#example","title":"Example","text":"<pre><code>axim inspect model.axim\n</code></pre> <p>Use before serving</p> <p>Run <code>axim inspect</code> on a freshly exported or downloaded <code>.axim</code> to confirm it contains weights, config, and tokenizer before serving or fine-tuning.</p>"},{"location":"cli/prepare-data/","title":"axim prepare-data","text":"<p>Download a dataset from the Hugging Face Hub and prepare train/validation JSONL files for SFT.</p>"},{"location":"cli/prepare-data/#usage","title":"Usage","text":"<pre><code>axim prepare-data --repo &lt;repo&gt; --output-train &lt;path&gt; --output-val &lt;path&gt; [flags]\n</code></pre>"},{"location":"cli/prepare-data/#flags","title":"Flags","text":"Flag Default Description <code>--repo</code> required Hugging Face dataset repository id <code>--config</code> <code>None</code> Dataset config name to load <code>--split</code> <code>train</code> Dataset split to use <code>--train-examples</code> <code>2000</code> Number of examples to write to the training split <code>--val-examples</code> <code>200</code> Number of examples to write to the validation split <code>--output-train</code> required Output path for training JSONL <code>--output-val</code> required Output path for validation JSONL <code>--seed</code> <code>42</code> Random seed used for example selection <code>--system-prompt</code> <code>None</code> Default system prompt to prepend to records without one"},{"location":"cli/prepare-data/#example","title":"Example","text":"<pre><code>axim prepare-data \\\n    --repo my-org/chat-dataset \\\n    --config default \\\n    --split train \\\n    --train-examples 2000 \\\n    --val-examples 200 \\\n    --output-train data/train.jsonl \\\n    --output-val data/val.jsonl \\\n    --system-prompt \"You are a helpful coding assistant.\"\n</code></pre> <p>Dataset format</p> <p>The downstream SFT trainer accepts JSONL records with <code>messages</code>, <code>prompt</code>/<code>completion</code>, <code>instruction</code>/<code>input</code>/<code>output</code>, or plain <code>text</code>. The <code>prepare-data</code> command renders records into one of those chat formats depending on the source dataset schema.</p>"},{"location":"cli/serve/","title":"axim serve","text":"<p>Serve an <code>.axim</code> model behind an OpenAI-compatible HTTP API.</p>"},{"location":"cli/serve/#usage","title":"Usage","text":"<pre><code>axim serve --axim &lt;path&gt; [--host &lt;host&gt;] [--port &lt;port&gt;] [--device &lt;device&gt;]\n</code></pre>"},{"location":"cli/serve/#flags","title":"Flags","text":"Flag Default Description <code>--axim</code> required Path to the <code>.axim</code> package to load <code>--host</code> <code>0.0.0.0</code> Network interface to bind <code>--port</code> <code>None</code> TCP port; when omitted, the server picks a random free port <code>--device</code> <code>cpu</code> Device to load the model on (<code>cpu</code>, <code>cuda</code>, <code>mps</code>, etc.)"},{"location":"cli/serve/#example","title":"Example","text":"<pre><code>axim serve --axim model.axim --host 127.0.0.1 --port 8080 --device cuda\n</code></pre> <p>Request logging</p> <p>The server prints a startup line with the bound host/port and a timestamped block for every request:</p> <pre><code>server: http://0.0.0.0:8080\n[14:02:31] #   1 \u2192 POST /v1/chat/completions  prompt=128tok  msgs=4  stream=on  max_tokens=256\n[14:02:33] #   1 \u2190 done  87tok in 2.04s (42.6 tok/s)  finish=stop (&lt;|assistant_end|&gt; generated)\n</code></pre> <p>OpenAI-compatible endpoints</p> <p>Use the displayed host:port in clients that expect an OpenAI base URL, for example <code>http://127.0.0.1:8080/v1</code>.</p>"},{"location":"cli/sft/","title":"axim sft","text":"<p>Fine-tune an <code>.axim</code> model on supervised chat data.</p> <p><code>axim sft</code> does not use the shared subparser. It short-circuits to the SFT trainer's own <code>argparse</code>, so the full flag set and defaults are shown with:</p> <pre><code>axim sft --help\n</code></pre>"},{"location":"cli/sft/#usage","title":"Usage","text":"<pre><code>axim sft [--config &lt;path&gt;] [flags...]\n</code></pre>"},{"location":"cli/sft/#common-flags","title":"Common flags","text":"Flag Default Description <code>--config</code> <code>None</code> JSON or YAML config file; CLI flags override it <code>--base-model</code> <code>model.axim</code> Base <code>.axim</code> to fine-tune, or a directory with <code>model.safetensors</code> + <code>config.json</code> <code>--train</code> <code>None</code> Training JSONL path <code>--val</code> <code>None</code> Validation JSONL path (optional; otherwise a slice of <code>--train</code> is held out) <code>--output</code> <code>finetuned.axim</code> Output <code>.axim</code> path <code>--device</code> autodetect <code>cuda</code>, <code>cpu</code>, <code>mps</code>, or empty for auto <code>--dtype</code> autodetect <code>bfloat16</code>, <code>float16</code>, <code>float32</code>, or empty for auto <code>--epochs</code> <code>3</code> Number of passes over the training data <code>--batch-size</code> <code>4</code> Micro-batch size <code>--grad-accum</code> <code>8</code> Gradient accumulation steps <code>--optimizer</code> <code>muon</code> <code>muon</code> (MuonAdamW) or <code>adamw</code> <code>--lr</code> <code>None</code> Base learning rate <code>--max-grad-norm</code> <code>1.0</code> Gradient clipping max norm; <code>0</code> disables <p>Full flag list</p> <p>The trainer exposes many more knobs for learning-rate schedules, sampling, checkpointing, logging, and distributed training. See <code>axim sft --help</code> for the complete list.</p>"},{"location":"cli/sft/#quick-3-step-recipe","title":"Quick 3-step recipe","text":"<ol> <li> <p>Download a base model:</p> <pre><code>axim download --repo my-org/my-model --output model.axim\n</code></pre> </li> <li> <p>Prepare SFT data:</p> <pre><code>axim prepare-data \\\n    --repo my-org/chat-dataset \\\n    --output-train data/train.jsonl \\\n    --output-val data/val.jsonl\n</code></pre> </li> <li> <p>Fine-tune:</p> <pre><code>axim sft --base-model model.axim \\\n    --train data/train.jsonl \\\n    --val data/val.jsonl \\\n    --output finetuned.axim \\\n    --device cuda --epochs 3\n</code></pre> </li> </ol> <p>Resume training</p> <p>Pass <code>--resume-from &lt;checkpoint.axim&gt;</code> or <code>--resume-from &lt;out_dir&gt;</code> to continue from a saved checkpoint. Resuming from a directory restores optimizer state when <code>--checkpoint-every</code> was used.</p>"},{"location":"development/roadmap/","title":"Roadmap and limitations","text":"<p>This page tracks what axim currently does and does not do.</p>"},{"location":"development/roadmap/#current-limitations","title":"Current limitations","text":"<ul> <li>nanoChat-only \u2014 The inference and SFT code is built around the nanoChat   architecture (value embeddings, smear, backout, ReLU^2). Models using other   backbones are not supported.</li> <li>No KV cache \u2014 The API server and sampling loop run a full forward pass for   every token. Fine for small models on a GPU, slow on CPU or at long contexts.</li> <li>No quantization \u2014 Weights are stored at full precision (fp32/bf16). 4-bit or   8-bit quantization is not implemented.</li> <li>RustBPETokenizer-only \u2014 The tokenizer must be the tiktoken/RustBPETokenizer   used by nanoChat. Other tokenizers need adapter code.</li> </ul>"},{"location":"development/roadmap/#roadmap","title":"Roadmap","text":"<ul> <li>[ ] KV cache for faster inference</li> <li>[ ] Quantization support (GGUF-style Q4_K_M, etc.)</li> <li>[x] Streaming tokenizer (decode tokens as they arrive)</li> <li>[ ] Multi-model support in the API server</li> <li>[ ] Model zoo / registry</li> <li>[ ] Digital signatures for <code>.axim</code> files</li> </ul>"},{"location":"format/spec/","title":"AXIM v1 Format Specification","text":"<p><code>.axim</code> is a binary package format for nanoChat-style models. A single file contains model weights, architecture config, tokenizer, and metadata.</p>"},{"location":"format/spec/#design-goals","title":"Design goals","text":"<ul> <li>Single file: everything needed for inference or fine-tuning is inside.</li> <li>Memory-mappable weights: the weights section is a raw safetensors blob, so it loads as fast as safetensors.</li> <li>Small metadata overhead: the header + section table is typically a few hundred bytes.</li> <li>Simple parsing: the file is read sequentially once the section table is known.</li> </ul>"},{"location":"format/spec/#file-layout","title":"File layout","text":"<pre><code>[Header]            13 bytes\n  magic: \"AXIM\\0\"    5 bytes\n  version: uint32     4 bytes\n  flags: uint32       4 bytes\n\n[Section Table]     variable\n  num_sections: uint32\n  sections[]:\n    type:   uint32    (1=weights, 2=config, 3=tokenizer, 4=metadata)\n    offset: uint64    absolute byte offset of section data\n    size:   uint64    length of section data in bytes\n    name_len: uint32  length of section name in UTF-8 bytes\n    name:   UTF-8     variable-length section name\n\n[Section Data]      aligned to 8-byte boundaries\n  weights:   safetensors blob\n  config:    JSON text\n  tokenizer: pickle bytes\n  metadata:  JSON text\n</code></pre>"},{"location":"format/spec/#header-byte-layout","title":"Header byte layout","text":"Offset Size Field Value 0 5 magic <code>AXIM\\0</code> (bytes) 5 4 version <code>1</code> (uint32, little-endian) 9 4 flags <code>0</code> (uint32, little-endian) <p>Total header size: 13 bytes.</p>"},{"location":"format/spec/#section-table-entry","title":"Section table entry","text":"<p>Each entry is exactly 24 bytes of fixed fields plus the variable-length name:</p> Offset in entry Size Field 0 4 type (uint32) 4 8 offset (uint64) 12 8 size (uint64) 20 4 name_len (uint32) 24 name_len name (UTF-8)"},{"location":"format/spec/#section-types","title":"Section types","text":"Value Name Stored data 1 <code>WEIGHTS</code> safetensors blob \u2014 <code>model.safetensors</code> 2 <code>CONFIG</code> JSON text \u2014 <code>config.json</code> 3 <code>TOKENIZER</code> pickle bytes \u2014 <code>tokenizer.pkl</code> 4 <code>METADATA</code> JSON text \u2014 <code>metadata.json</code>"},{"location":"format/spec/#alignment","title":"Alignment","text":"<p>After the section table, padding bytes are inserted so the first section starts on an 8-byte boundary. Likewise, each section is padded so the next section also starts on an 8-byte boundary. Sections themselves are stored contiguously after the table.</p> <p>Why 8-byte alignment?</p> <p>Keeping section starts on 8-byte boundaries makes the file friendlier to memory mapping and direct pointer arithmetic, and it keeps the safetensors blob aligned the way safetensors expects.</p>"},{"location":"format/spec/#reading-an-axim-file","title":"Reading an <code>.axim</code> file","text":"<ol> <li>Verify the 5-byte magic is <code>AXIM\\0</code>.</li> <li>Read <code>version</code> and <code>flags</code> (currently version must be <code>1</code>, flags must be <code>0</code>).</li> <li>Read <code>num_sections</code>.</li> <li>For each section, read <code>type</code>, <code>offset</code>, <code>size</code>, and <code>name</code>.</li> <li>Seek to each section's <code>offset</code>, read <code>size</code> bytes, and deserialize based on <code>type</code>:</li> <li><code>WEIGHTS</code> \u2192 safetensors load</li> <li><code>CONFIG</code> \u2192 JSON parse</li> <li><code>TOKENIZER</code> \u2192 pickle load</li> <li><code>METADATA</code> \u2192 JSON parse</li> </ol>"},{"location":"format/spec/#writing-an-axim-file","title":"Writing an <code>.axim</code> file","text":"<ol> <li>Serialize each section independently:</li> <li>Weights via <code>safetensors.torch.save_file</code> into a temporary file, then read back as bytes.</li> <li>Config as UTF-8 JSON.</li> <li>Tokenizer as raw pickle bytes.</li> <li>Metadata as UTF-8 JSON, if present.</li> <li>Compute the total header + section table size.</li> <li>Add padding so the first data section is aligned to 8 bytes.</li> <li>Compute each section's absolute offset, adding padding between sections.</li> <li>Write the header, section table, padding, and then each section's raw bytes.</li> </ol>"},{"location":"format/spec/#limits-and-notes","title":"Limits and notes","text":"<ul> <li>Section names are stored as raw UTF-8 bytes, not null-terminated.</li> <li>All integer fields are little-endian.</li> <li>The format currently supports only version <code>1</code> and flags <code>0</code>.</li> <li>The weights section can be loaded directly with <code>safetensors.torch.load</code> because it is a complete safetensors file in memory.</li> </ul>"},{"location":"format/why-axim/","title":"Why <code>.axim</code>?","text":"<p>There are plenty of ways to ship a language model. We picked <code>.axim</code> because the existing options all made us do extra work.</p>"},{"location":"format/why-axim/#vs-safetensors","title":"vs. Safetensors","text":"<p>Safetensors is excellent. It is fast, safe, and the weights section inside an <code>.axim</code> file is a safetensors blob. But safetensors is just weights. You still need a <code>config.json</code> to know the architecture, a <code>tokenizer.pkl</code> to turn text into tokens, and maybe a metadata file with training provenance.</p> <p>Before <code>.axim</code>, that meant juggling four or five files across downloads, uploads, and inference scripts. <code>.axim</code> keeps the safetensors speed and adds a tiny header so the rest of the model travels with it.</p>"},{"location":"format/why-axim/#vs-gguf","title":"vs. GGUF","text":"<p>GGUF is the de facto standard for llama.cpp-style inference, and it works well for models that fit llama.cpp's architectural assumptions.</p> <p>nanoChat does not.</p> <p>The models this engine targets use custom pieces \u2014 value embeddings, smear, backout, ReLU\u00b2 activations \u2014 that llama.cpp does not understand. Converting to GGUF would mean either stripping those pieces out or fighting the format, neither of which is useful. <code>.axim</code> is built around the actual architecture instead of bending the architecture to fit a format.</p>"},{"location":"format/why-axim/#vs-zip-tar","title":"vs. Zip / Tar","text":"<p>A zip file could absolutely hold the same four blobs. It would even compress things.</p> <p>But <code>.axim</code> is purpose-built: a fixed header, a typed section table, and aligned offsets mean you can inspect and load the file without a general-purpose archive parser. The weights section is exposed as raw safetensors data, so it can be memory-mapped directly. And honestly, a custom binary format is cooler.</p>"},{"location":"format/why-axim/#what-axim-optimizes-for","title":"What <code>.axim</code> optimizes for","text":"<ul> <li>One file to move around \u2014 download, copy, or serve from a single path.</li> <li>Fast loading \u2014 header + table is ~200 bytes, weights load through safetensors.</li> <li>Honest architecture support \u2014 no pretending a nanoChat model is something it is not.</li> <li>Simple tooling \u2014 a small Python module can read, write, and inspect the format in a few hundred lines.</li> </ul> <p>It is not trying to replace every model format. It is the right container for the Axiom model line.</p>"},{"location":"getting-started/installation/","title":"Installation","text":""},{"location":"getting-started/installation/#requirements","title":"Requirements","text":"<ul> <li>Python &gt;= 3.10</li> <li>A working Python environment with <code>pip</code></li> <li>Optional but recommended: a CUDA-capable GPU for inference and training</li> </ul> <p>The package is compatible with Windows, Linux, and macOS.</p>"},{"location":"getting-started/installation/#install-from-source","title":"Install from source","text":"<p>Clone the repository and install the package in editable mode:</p> <pre><code>git clone https://github.com/StrawberCar/Axiom-Inference-Engine.git\ncd Axiom-Inference-Engine\npip install -e .\n</code></pre> <p>This creates the <code>axim</code> console script and makes <code>python -m axim</code> work.</p> <p>Editable install</p> <p><code>pip install -e .</code> is the recommended install path while the project is in active development. It links the package into your environment so source changes take effect immediately.</p>"},{"location":"getting-started/installation/#optional-extras","title":"Optional extras","text":"<p>Install extra dependencies when you need them:</p> <pre><code># For the SFT pipeline (accelerate, tqdm)\npip install -e \".[sft]\"\n\n# For Flash Attention support\npip install -e \".[flash]\"\n</code></pre> <p>Flash Attention</p> <p>The <code>[flash]</code> extra pulls in <code>flash-attn</code>. This requires a compatible CUDA toolchain and may take a while to build. Only install it if your GPU supports the kernel.</p>"},{"location":"getting-started/installation/#about-the-vendored-nanochat","title":"About the vendored nanochat","text":"<p><code>axim/_nanochat/</code> is a vendored fork of Andrej Karpathy's nanochat (MIT license). It is included in the repository, so no separate clone is required. The package ships with everything needed to build, run, and fine-tune nanoChat-based models.</p>"},{"location":"getting-started/installation/#verify-the-install","title":"Verify the install","text":"<pre><code>axim --help\n</code></pre> <p>You should see the unified CLI and its subcommands: <code>serve</code>, <code>inspect</code>, <code>export</code>, <code>download</code>, <code>infer</code>, <code>prepare-data</code>, and <code>sft</code>.</p>"},{"location":"getting-started/quickstart/","title":"Quickstart","text":"<p>This guide walks you through installing the Axiom Inference Engine, downloading a base model, running local inference, and starting the OpenAI-compatible API server.</p>"},{"location":"getting-started/quickstart/#1-install-the-package","title":"1. Install the package","text":"<pre><code>pip install -e .\n</code></pre> <p>For the SFT pipeline extras, use <code>pip install -e \".[sft]\"</code>. See Installation for details.</p>"},{"location":"getting-started/quickstart/#2-download-a-base-model","title":"2. Download a base model","text":"<p>The fastest way to get a model is to pull one from the Hugging Face Hub:</p> <pre><code>axim download --repo Strawbercar/Axiom-V1-Base --output base.axim\n</code></pre> <p>This writes <code>base.axim</code> to the current directory, packaging weights, config, tokenizer, and metadata into a single file.</p> <p>Already an <code>.axim</code> on the Hub?</p> <p>If the repository already contains an <code>.axim</code> file, <code>axim download</code> copies it directly. Otherwise it stages safetensors shards, <code>config.json</code>, and <code>tokenizer.pkl</code>, then packs them locally.</p>"},{"location":"getting-started/quickstart/#3-run-local-inference","title":"3. Run local inference","text":"<p>Run a raw completion or a chat-formatted prompt:</p> <pre><code># Raw completion\naxim infer --axim base.axim --prompt \"Once upon a time\" --device cuda\n\n# Chat mode\naxim infer --axim base.axim --prompt \"Explain quantum computing in one sentence.\" --chat --device cuda\n</code></pre> <p>In chat mode the CLI renders the chat scaffold, appends <code>&lt;|assistant_start|&gt;</code>, and stops when <code>&lt;|assistant_end|&gt;</code> is generated. <code>--repetition-penalty</code>, <code>--temperature</code>, <code>--top-k</code>, and <code>--max-tokens</code> are applied automatically.</p> <p>Default device</p> <p>The default device is <code>cpu</code>. Pass <code>--device cuda</code> to use a GPU.</p>"},{"location":"getting-started/quickstart/#4-start-the-api-server","title":"4. Start the API server","text":"<pre><code>axim serve --axim base.axim --device cuda --port 8000\n</code></pre> <p>The server binds to <code>0.0.0.0</code> and picks a random free port if <code>--port</code> is omitted. The examples below assume <code>--port 8000</code>; use the logged port if you omit it.</p>"},{"location":"getting-started/quickstart/#5-send-a-request","title":"5. Send a request","text":"Linux / macOSWindows (PowerShell) <pre><code>curl http://localhost:8000/v1/chat/completions \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"model\": \"axim\",\n    \"messages\": [{\"role\": \"user\", \"content\": \"hello\"}],\n    \"max_tokens\": 50,\n    \"temperature\": 0.7,\n    \"stream\": true\n  }'\n</code></pre> <pre><code>Invoke-RestMethod -Uri \"http://localhost:8000/v1/chat/completions\" `\n  -Method POST `\n  -ContentType \"application/json\" `\n  -Body '{\n    \"model\": \"axim\",\n    \"messages\": [{\"role\": \"user\", \"content\": \"hello\"}],\n    \"max_tokens\": 50,\n    \"temperature\": 0.7\n  }'\n</code></pre> <p>Streaming</p> <p>Add <code>\"stream\": true</code> to receive token chunks via SSE. Non-streaming requests return the full completion in one response.</p>"},{"location":"getting-started/quickstart/#next-steps","title":"Next steps","text":"<ul> <li>Read the format spec to understand how <code>.axim</code> files are laid out.</li> <li>Open <code>webui/index.html</code> in a browser and point it at <code>http://localhost:8000</code>.</li> <li>Try the SFT pipeline to fine-tune a chat model.</li> </ul>"},{"location":"sft/","title":"SFT overview","text":"<p>The <code>axim sft</code> pipeline turns a base <code>.axim</code> model into a chat-tuned <code>.axim</code> model. The output is a drop-in replacement for the base model: it can be served with <code>axim serve</code>, inspected with <code>axim inspect</code>, loaded with <code>load_model</code>, or used in the web UI.</p>"},{"location":"sft/#what-the-pipeline-does","title":"What the pipeline does","text":"<ol> <li>Load a base <code>.axim</code> (weights + config + tokenizer + metadata).</li> <li>Normalize chat-formatted JSONL training data.</li> <li>Render conversations to token ids with assistant-only loss masking.</li> <li>Pack multiple conversations into fixed-length training rows (<code>bestfit</code> packing).</li> <li>Fine-tune with MuonAdamW or AdamW and a warmup-stable-decay learning-rate    schedule.</li> <li>Periodically evaluate, generate samples, and write checkpoints.</li> <li>Save the final merged model back to <code>.axim</code>.</li> </ol>"},{"location":"sft/#quick-path-notebook-colab","title":"Quick path (notebook / Colab)","text":"<p>The notebook at <code>notebooks/axim_sft_colab.ipynb</code> runs the whole pipeline one cell at a time on a single GPU:</p> <pre><code># 1. Download a base model\naxim download --repo Strawbercar/Axiom-V1-Base --output base.axim\n\n# 2. Build train/val JSONL from a Hugging Face dataset\naxim prepare-data --repo HuggingFaceTB/smol-smoltalk \\\n    --train-examples 50000 --val-examples 500 \\\n    --output-train train.jsonl --output-val val.jsonl\n\n# 3. Fine-tune\naxim sft --config configs/sft_example.json \\\n    --base-model base.axim --train train.jsonl --val val.jsonl \\\n    --output tuned.axim --device cuda --dtype bfloat16\n</code></pre>"},{"location":"sft/#sections","title":"Sections","text":"<ul> <li>Preparing data \u2014 dataset formats and <code>axim prepare-data</code>.</li> <li>Training \u2014 the <code>axim sft</code> command and training behavior.</li> <li>Config reference \u2014 every field in <code>configs/sft_example.json</code>.</li> </ul>"},{"location":"sft/config-reference/","title":"SFT config reference","text":"<p>This table describes every field used by <code>axim sft</code>. Defaults are the hardcoded fallback values in <code>axim/sft/train.py</code> when the field is not set by CLI or config.</p>"},{"location":"sft/config-reference/#io","title":"I/O","text":"Field Type Default Description <code>base_model</code> str <code>model.axim</code> Path to the base <code>.axim</code> or a directory with safetensors/config/tokenizer. <code>train</code> str / null <code>null</code> Training JSONL path. Required unless resuming. <code>val</code> str / null <code>null</code> Validation JSONL path. If null, a slice of <code>train</code> is held out. <code>val_frac</code> float <code>0.02</code> Fraction of training data to hold out when <code>val</code> is not given. <code>output</code> str <code>finetuned.axim</code> Final merged <code>.axim</code> output path. <code>out_dir</code> str / null <code>null</code> Checkpoint directory. Defaults to a <code>sft_checkpoints</code> folder next to <code>output</code>. <code>resume_from</code> str / null <code>null</code> Resume from a <code>.axim</code> or checkpoint directory."},{"location":"sft/config-reference/#device-and-precision","title":"Device and precision","text":"Field Type Default Description <code>device</code> str <code>\"\"</code> Device: <code>cuda</code>, <code>cpu</code>, <code>mps</code>. Empty string auto-detects. <code>dtype</code> str <code>\"\"</code> Compute dtype: <code>bfloat16</code>, <code>float16</code>, <code>float32</code>. Empty string auto-detects via nanoChat. <code>compile</code> int / bool <code>0</code> Whether to <code>torch.compile</code> the model. Off by default for portability. <code>cast_dtype</code> int / bool <code>0</code> Cast params/buffers to compute dtype after loading. Useful for GPUs that cannot run the stored dtype, e.g. bf16 weights on a T4."},{"location":"sft/config-reference/#data","title":"Data","text":"Field Type Default Description <code>max_seq_len</code> int / null <code>null</code> Training context length. Defaults to the base model's <code>max_position_embeddings</code> and is clamped to it. <code>train_on</code> str <code>\"assistant\"</code> Tokens to supervise: <code>\"assistant\"</code> or <code>\"all\"</code>. <code>pack</code> str <code>\"bestfit\"</code> Packing mode: <code>\"bestfit\"</code> or <code>\"single\"</code>. <code>system_prompt</code> str / null <code>null</code> System prompt prepended to records that lack one. <code>seed</code> int <code>42</code> RNG seed for shuffling and splits."},{"location":"sft/config-reference/#training","title":"Training","text":"Field Type Default Description <code>batch_size</code> int <code>4</code> Micro-batch rows per forward pass. <code>grad_accum</code> int <code>8</code> Gradient accumulation steps. <code>epochs</code> float <code>3</code> Training epochs. <code>-1</code> disables and uses <code>steps</code>. <code>steps</code> int <code>-1</code> Max optimizer steps. <code>-1</code> means derive from <code>epochs</code>. <code>max_grad_norm</code> float <code>1.0</code> Gradient clipping max norm. <code>0</code> disables."},{"location":"sft/config-reference/#optimizer","title":"Optimizer","text":"Field Type Default Description <code>optimizer</code> str <code>\"muon\"</code> Optimizer: <code>\"muon\"</code> or <code>\"adamw\"</code>. <code>lr</code> float / null <code>null</code> Base LR for AdamW. Muon ignores this and uses the group LRs below. <code>matrix_lr</code> float <code>0.02</code> Muon LR for transformer matrices. <code>embedding_lr</code> float <code>0.3</code> Muon LR for token embeddings. <code>unembedding_lr</code> float <code>0.004</code> Muon LR for the output lm_head. <code>scalar_lr</code> float <code>0.5</code> Muon LR for per-layer scalars. <code>weight_decay</code> float <code>0.0</code> Weight decay. Applied to matrices for Muon; all params for AdamW. <code>emb_lr_mult</code> float <code>1.0</code> AdamW embedding LR multiplier relative to <code>--lr</code>."},{"location":"sft/config-reference/#lr-schedule","title":"LR schedule","text":"Field Type Default Description <code>init_lr_frac</code> float <code>0.5</code> LR at step 0, as a fraction of the base LR. <code>warmup_ratio</code> float <code>0.03</code> Fraction of training devoted to linear warmup. <code>warmdown_ratio</code> float <code>0.2</code> Fraction of training devoted to linear warmdown. <code>final_lr_frac</code> float <code>0.0</code> Final LR as a fraction of base LR; the warmdown floor."},{"location":"sft/config-reference/#logging-and-checkpointing","title":"Logging and checkpointing","text":"Field Type Default Description <code>eval_every</code> int <code>200</code> Run validation every N steps. <code>-1</code> disables. <code>eval_steps</code> int <code>20</code> Number of validation batches per evaluation. <code>sample_every</code> int <code>500</code> Generate sample completions every N steps. <code>-1</code> disables. <code>sample_prompts</code> int <code>3</code> Number of validation prompts to sample at each <code>sample_every</code>. <code>sample_tokens</code> int <code>64</code> Max tokens per sample generation. <code>sample_static_file</code> str / null <code>null</code> JSONL of fixed prompts sampled every <code>sample_every</code>. <code>sample_random</code> int / null <code>null</code> Number of random validation prompts to sample each time. <code>save_every</code> int <code>-1</code> Write a numbered <code>.axim</code> snapshot every N steps. <code>-1</code> disables. <code>checkpoint_every</code> int <code>-1</code> Write resumable <code>checkpoint.axim</code> + <code>checkpoint.pt</code> every N steps. <code>-1</code> disables. <code>save_optim</code> int / bool <code>0</code> Persist optimizer state with the final model. <code>wandb</code> str <code>\"dummy\"</code> WandB run name. <code>\"dummy\"</code> or empty disables logging. <code>log_every</code> int <code>1</code> Console log interval in steps."},{"location":"sft/preparing-data/","title":"Preparing SFT data","text":"<p>SFT training consumes one JSON object per line. The canonical format is nanoChat-style chat JSONL:</p> <pre><code>{\"messages\": [{\"role\": \"user\", \"content\": \"What is 2+2?\"}, {\"role\": \"assistant\", \"content\": \"2+2 is 4.\"}]}\n{\"messages\": [{\"role\": \"user\", \"content\": \"Say hello\"}, {\"role\": \"assistant\", \"content\": \"Hello!\"}]}\n</code></pre>"},{"location":"sft/preparing-data/#axim-prepare-data","title":"<code>axim prepare-data</code>","text":"<p>Download and normalize a dataset from the Hugging Face Hub:</p> <pre><code>axim prepare-data --repo HuggingFaceTB/smol-smoltalk \\\n    --train-examples 2000 --val-examples 200 \\\n    --output-train train.jsonl --output-val val.jsonl \\\n    --system-prompt \"You are a helpful coding assistant.\"\n</code></pre> <p><code>prepare-data</code> recognizes several common dataset shapes and normalizes them to <code>{\"messages\": [...]}</code>:</p> <ul> <li><code>messages</code> \u2014 passed through.</li> <li><code>conversations</code> \u2014 ShareGPT style with <code>from</code>/<code>value</code> mapped to <code>role</code>/<code>content</code>.</li> <li><code>instruction</code> + <code>output</code> (+ optional <code>input</code>) \u2014 Alpaca style.</li> <li><code>problem</code> + <code>solution</code> (+ optional <code>reasoning</code>).</li> <li><code>question</code> + <code>answer</code>.</li> <li><code>prompt</code> + <code>completion</code>.</li> <li><code>text</code> \u2014 treated as a single assistant turn with an empty user turn.</li> </ul> <p>Records are filtered to keep only rows that contain at least one user message and one assistant message.</p>"},{"location":"sft/preparing-data/#conversation-format","title":"Conversation format","text":"<p>Every training record must end up as:</p> <pre><code>{\n  \"messages\": [\n    {\"role\": \"user\", \"content\": \"...\"},\n    {\"role\": \"assistant\", \"content\": \"...\"}\n  ]\n}\n</code></pre> <p>A leading <code>system</code> message is allowed. Conversations must alternate <code>user</code> / <code>assistant</code> starting with <code>user</code>.</p> <p>Assistant content may also be a list of typed parts:</p> <pre><code>{\"role\": \"assistant\", \"content\": [\n  {\"type\": \"text\", \"text\": \"...\"},\n  {\"type\": \"python\", \"text\": \"print(1)\"},\n  {\"type\": \"python_output\", \"text\": \"1\"}\n]}\n</code></pre>"},{"location":"sft/preparing-data/#normalize_record-behavior","title":"<code>normalize_record</code> behavior","text":"<p>During training, each JSONL object is passed through <code>normalize_record</code> in <code>axim/sft/data.py</code>. It accepts these shapes:</p> <ul> <li><code>{\"messages\": [...]}</code></li> <li><code>{\"prompt\": \"...\", \"completion\": \"...\"}</code></li> <li><code>{\"instruction\": \"...\", \"output\": \"...\", \"input\"?: \"...\"}</code></li> <li><code>{\"text\": \"...\"}</code></li> </ul> <p>For all shapes, a <code>default_system</code> prompt can be injected when the record lacks a leading system message.</p>"},{"location":"sft/preparing-data/#system-prompt-injection","title":"System prompt injection","text":"<p>Use the <code>--system-prompt</code> CLI flag (or <code>system_prompt</code> in the config file) to prepend a system message to every record that does not already have one:</p> <pre><code>axim sft --system-prompt \"You are a helpful assistant.\" ...\n</code></pre> <p>The system message is merged into the first user turn when rendering (nanoChat convention), so it does not appear as a separate scaffold token sequence during inference.</p>"},{"location":"sft/preparing-data/#train_on-assistant-vs-all","title":"<code>train_on</code> \u2014 assistant vs all","text":"<p>The <code>train_on</code> config field controls which positions contribute to the loss:</p> <ul> <li><code>\"assistant\"</code> (default) \u2014 only assistant content tokens are supervised. User   turns, special scaffold tokens, and padding are masked to <code>-1</code> (ignored).</li> <li><code>\"all\"</code> \u2014 every content token is supervised, including user turns. Useful for   continued pre-training on chat-formatted data.</li> </ul> <p>This is implemented by <code>render_conversation</code> in <code>axim/sft/data.py</code>, which emits <code>(ids, mask)</code> pairs where <code>mask == 1</code> marks supervised positions.</p>"},{"location":"sft/training/","title":"Training with <code>axim sft</code>","text":"<p>Run the fine-tuner:</p> <pre><code>axim sft --config configs/sft_example.json \\\n    --base-model base.axim --train train.jsonl --val val.jsonl \\\n    --output tuned.axim --device cuda --dtype bf16\n</code></pre> <p>Every knob can also be passed directly as a CLI flag; see <code>axim sft --help</code>.</p>"},{"location":"sft/training/#config-resolution","title":"Config resolution","text":"<p>For each option the trainer uses the first available value in this order:</p> <ol> <li>CLI flag \u2014 e.g. <code>--batch-size 8</code>.</li> <li>Config file \u2014 e.g. <code>\"batch_size\": 8</code> in the JSON config.</li> <li>Hardcoded default \u2014 defined in <code>axim/sft/train.py</code>.</li> </ol>"},{"location":"sft/training/#base-model-loading","title":"Base model loading","text":"<p>The trainer loads <code>base_model</code> with <code>_load_base</code>, which supports:</p> <ul> <li><code>.axim</code> files \u2014 via <code>axim.core.load_axim</code>.</li> <li>A directory with <code>model.safetensors</code> + <code>config.json</code> + <code>tokenizer.pkl</code>.</li> </ul> <p><code>max_seq_len</code> defaults to the base model's <code>max_position_embeddings</code> and is clamped to that value if a larger value is requested.</p>"},{"location":"sft/training/#data-packing","title":"Data packing","text":"<p>The rendered conversations are packed into fixed-length rows by <code>PackedBatcher</code>:</p> <ul> <li><code>\"bestfit\"</code> (default) \u2014 greedily bin-packs conversations into each row and pads   the remainder with BOS tokens. Mirrors nanoChat's SFT packing and wastes no tokens.</li> <li><code>\"single\"</code> \u2014 one conversation per row, truncated and padded. Simpler but less   efficient.</li> </ul> <p>Targets are int64 tensors with <code>-1</code> at padded/unsupervised positions so they are ignored by cross-entropy. Inputs are targets shifted right with a BOS prefix.</p>"},{"location":"sft/training/#assistant-only-masking","title":"Assistant-only masking","text":"<p>When <code>train_on=\"assistant\"</code> (default), only assistant content tokens are supervised. Special scaffold tokens (<code>&lt;|user_start|&gt;</code>, <code>&lt;|assistant_start|&gt;</code>, etc.) and user content are masked to <code>-1</code>. Use <code>train_on=\"all\"</code> to supervise every content token.</p>"},{"location":"sft/training/#optimizer","title":"Optimizer","text":""},{"location":"sft/training/#muonadamw-default","title":"MuonAdamW (default)","text":"<pre><code>axim sft --optimizer muon \\\n    --matrix-lr 0.02 --embedding-lr 0.3 --unembedding-lr 0.004 --scalar-lr 0.5\n</code></pre> <p>Muon uses four parameter groups:</p> <ul> <li><code>matrix</code> \u2014 transformer block matrices.</li> <li><code>embedding</code> \u2014 token embeddings.</li> <li><code>unembedding</code> \u2014 <code>lm_head</code>.</li> <li><code>scalar</code> \u2014 per-layer scalars.</li> </ul> <p>During the first 300 steps Muon momentum ramps from <code>0.85</code> to <code>0.95</code>.</p>"},{"location":"sft/training/#adamw","title":"AdamW","text":"<pre><code>axim sft --optimizer adamw --lr 3e-4 --emb-lr-mult 1.0\n</code></pre> <p>AdamW uses two parameter groups: embeddings at <code>lr * emb_lr_mult</code> and everything else at <code>lr</code>. Weight decay applies to all trainable parameters.</p> <p>If Muon fails to initialize (e.g. on CPU/MPS), the trainer automatically falls back to AdamW.</p>"},{"location":"sft/training/#learning-rate-schedule-wsd","title":"Learning-rate schedule (WSD)","text":"<p>The default schedule is warmup-stable-decay:</p> <ul> <li><code>init_lr_frac</code> (default <code>0.5</code>) \u2014 LR at step 0 as a fraction of the base LR.</li> <li><code>warmup_ratio</code> (default <code>0.03</code>) \u2014 fraction of training spent linearly warming up   from <code>init_lr_frac</code> to <code>1.0</code>.</li> <li><code>warmdown_ratio</code> (default <code>0.2</code>) \u2014 fraction of training spent linearly decaying   to <code>final_lr_frac</code>.</li> <li><code>final_lr_frac</code> (default <code>0.0</code>) \u2014 LR floor at the end of training.</li> </ul> <p><code>--lr</code> is used by AdamW; Muon always uses the four group LRs (<code>--matrix-lr</code>, <code>--embedding-lr</code>, <code>--unembedding-lr</code>, <code>--scalar-lr</code>).</p>"},{"location":"sft/training/#batch-sizing","title":"Batch sizing","text":"<pre><code>total_batch = batch_size * max_seq_len * grad_accum * world_size\n</code></pre> <ul> <li><code>batch_size</code> \u2014 micro-batch rows per forward pass.</li> <li><code>grad_accum</code> \u2014 gradient accumulation steps.</li> <li>Multi-GPU training is supported via <code>torchrun</code>, which scales the total batch by   <code>world_size</code>.</li> </ul>"},{"location":"sft/training/#in-training-sampling","title":"In-training sampling","text":"<p>The trainer can generate sample completions during training to watch quality improve:</p> <ul> <li><code>sample_every</code> (default <code>500</code>) \u2014 generate samples every N steps.</li> <li><code>sample_prompts</code> (default <code>3</code>) \u2014 number of validation prompts to sample.</li> <li><code>sample_tokens</code> (default <code>64</code>) \u2014 max tokens per sample.</li> <li><code>sample_static_file</code> \u2014 JSONL of fixed prompts; the same prompts are used every   sample so you can compare outputs across checkpoints.</li> <li><code>sample_random</code> \u2014 number of random validation prompts to sample each time   (re-seeded by step for variety).</li> </ul>"},{"location":"sft/training/#validation","title":"Validation","text":"<p>If <code>--val</code> is not provided, a deterministic slice of the training data is held out using <code>val_frac</code> (default <code>0.02</code>). Evaluation runs every <code>eval_every</code> steps (default <code>200</code>) over <code>eval_steps</code> batches (default <code>20</code>). A baseline validation loss is computed before training starts.</p>"},{"location":"sft/training/#checkpoints-and-saving","title":"Checkpoints and saving","text":"<ul> <li><code>save_every</code> \u2014 write a numbered full <code>.axim</code> snapshot (<code>step{N}.axim</code>) every N   steps. Heavy because each is a full model copy.</li> <li><code>checkpoint_every</code> \u2014 write a resumable <code>checkpoint.axim</code> + <code>checkpoint.pt</code>   (optimizer state) every N steps. Required for <code>--resume-from</code> to restore   optimizer state.</li> <li><code>save_optim</code> \u2014 persist optimizer state alongside the final model.</li> <li><code>resume_from</code> \u2014 resume weights (and optimizer state if available) from a <code>.axim</code>   or a checkpoint directory.</li> </ul> <p>The default output directory is next to <code>--output</code>, e.g. <code>out/sft_checkpoints</code>.</p>"},{"location":"sft/training/#final-merged-axim","title":"Final merged <code>.axim</code>","text":"<p>At the end of training the trainer writes the full merged model to <code>--output</code>. The output file contains:</p> <ul> <li>updated weights,</li> <li>the original config plus <code>training_step</code>, <code>val_loss</code>, and <code>finetuned=True</code>,</li> <li>the tokenizer,</li> <li>metadata including SFT provenance and the resolved training config.</li> </ul> <p>The output is a single, ready-to-serve <code>.axim</code> file.</p>"}]}