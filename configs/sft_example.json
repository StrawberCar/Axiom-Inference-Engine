{
  "_comment": "Example SFT config for an .axim model. Any field here can be overridden on the CLI. Run with: python scripts/sft_train.py --config configs/sft_example.json. NOTE: the Muon LRs below are ~1/5 of pretraining scale - a base model is already near its chat-data optimum, so pretraining-scale LRs make loss GO UP (catastrophic forgetting). Raise if too slow, lower if loss rises.",

  "base_model": "model.axim",
  "train": "data/example_sft.jsonl",
  "val": null,
  "val_frac": 0.1,
  "output": "out/finetuned.axim",
  "out_dir": "out/sft_checkpoints",

  "device": "",
  "dtype": "",
  "compile": 0,

  "max_seq_len": 2048,
  "train_on": "assistant",
  "pack": "bestfit",
  "system_prompt": null,
  "seed": 42,

  "batch_size": 4,
  "grad_accum": 8,
  "epochs": 3,
  "steps": -1,
  "max_grad_norm": 1.0,

  "optimizer": "muon",
  "matrix_lr": 0.004,
  "embedding_lr": 0.06,
  "unembedding_lr": 0.0008,
  "scalar_lr": 0.1,
  "weight_decay": 0.0,

  "init_lr_frac": 0.5,
  "warmup_ratio": 0.05,
  "warmdown_ratio": 0.2,
  "final_lr_frac": 0.0,

  "eval_every": 200,
  "eval_steps": 20,
  "sample_every": 500,
  "sample_prompts": 3,
  "sample_tokens": 64,
  "save_every": -1,
  "checkpoint_every": -1,
  "save_optim": 0,
  "resume_from": null,
  "wandb": "dummy",
  "log_every": 1
}